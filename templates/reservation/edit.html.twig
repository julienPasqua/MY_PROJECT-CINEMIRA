{% extends 'base.html.twig' %}

{% block title %}Modifier la réservation{% endblock %}

{% block stylesheets %}
<style>
    body {
        background: radial-gradient(circle at 50% 50%, #96cdff 0%, #469cf9 50%, #006ed4 100%);
        font-family: 'Futura', sans-serif;
    }

    .card-resa {
        background: white;
        border-radius: 15px;
        padding: 25px;
        box-shadow: 0 6px 20px rgba(0,0,0,0.25);
        max-width: 700px;
        margin: 40px auto;
    }

    .resa-title {
        text-align: center;
        color: #ffd700;
        font-size: 2rem;
        font-weight: bold;
        margin-bottom: 20px;
        text-shadow: 0 2px 5px rgba(0,0,0,0.3);
    }

    .resa-info strong {
        color: #0056b3;
    }

    .btn-danger {
        width: 100%;
        padding: 12px;
        font-size: 1.1rem;
        border-radius: 10px;
    }

    .btn-back {
        display: inline-block;
        margin-top: 15px;
        color: #fff;
        text-decoration: none;
        font-weight: bold;
    }
</style>
{% endblock %}

{% block body %}
<div class="card-resa">

    <div class="resa-title">
        Modifier la réservation
    </div>

    <p class="text-center mb-4">
        Voici les détails de votre réservation.  
        Vous pouvez **uniquement annuler** cette réservation.
    </p>

    <div class="resa-info">
        <p><strong>🎬 Film :</strong> {{ reservation.seance.film.titre }}</p>
        <p><strong>🏛 Cinéma :</strong> {{ reservation.seance.salle.cinema.nom }}</p>
        <p><strong>🕒 Séance :</strong> {{ reservation.seance.dateSeance|date("d/m/Y") }} à {{ reservation.seance.heureDebut|date("H:i") }}</p>
        <p><strong>💺 Places :</strong> {{ reservation.nombresPlaces }}</p>
        <p><strong>💰 Prix total :</strong> {{ reservation.prixTotal }} €</p>
        <p><strong>🔑 Code confirmation :</strong> {{ reservation.codeConfirmation }}</p>
    </div>

    <hr>

    <form method="post" action="{{ path('app_reservation_cancel', {id: reservation.id}) }}">
        <input type="hidden" name="_token" value="{{ csrf_token('cancel_resa_' ~ reservation.id) }}">

        <button class="btn btn-danger">
            ❌ Annuler la réservation
        </button>
    </form>

    <a href="{{ path('app_reservation_index') }}" class="btn-back">⬅ Retour à mes réservations</a>
</div>
{% endblock %}
