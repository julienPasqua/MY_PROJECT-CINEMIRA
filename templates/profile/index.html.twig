{% extends 'base.html.twig' %}

{% block title %}Mon Compte{% endblock %}

{% block body %}
<style>
    .btn-admin {
    background: linear-gradient(135deg, #ffd700, #ffed4e);
    color: #000;
    padding: 1rem 2rem;
    border-radius: 10px;
    font-weight: bold;
    text-decoration: none;
    display: inline-block;
    margin-left: 1rem;
}

.btn-admin:hover {
    background: linear-gradient(135deg, #ffed4e, #ffd700);
    transform: translateY(-2px);
    color: #000;
}
</style>

<div class="container py-5">

    <div class="card shadow-lg border-0 bg-dark text-light" style="border-radius: 12px;">
        <div class="card-header bg-warning text-dark fw-bold" style="border-radius: 12px 12px 0 0;">
            🎬 Mon Espace Personnel
        </div>

        <div class="card-body">

            <h4 class="mb-4">Informations du compte</h4>

            <div class="row mb-3">
                <div class="col-md-4 fw-bold">Email :</div>
                <div class="col-md-8">{{ user.email }}</div>
            </div>

            <div class="row mb-3">
                <div class="col-md-4 fw-bold">Rôle :</div>
                <div class="col-md-8">
                    {% for role in user.roles %}
                        <span class="badge bg-warning text-dark me-1">{{ role }}</span>
                    {% endfor %}
                </div>
            </div>

            <hr class="border-secondary my-4">

            <h5 class="text-warning">Actions rapides</h5>

            <div class="d-flex flex-wrap gap-3 mt-3">

                <a href="{{ path('app_logout') }}" class="btn btn-danger">
                    Se déconnecter
                </a>

                {# <a href="{{ path('cinema_new') }}" class="btn btn-outline-warning">
                    ➕ Ajouter un cinéma
                </a> #}

                {# <a href="{{ path('app_change_password') }}" class="btn btn-outline-light">
                    🔑 Changer mon mot de passe
                </a> #}

            </div>

        </div>
    </div>

</div>

{# Accès admin si l'utilisateur a le rôle ADMIN #}
{% if is_granted('ROLE_ADMIN') %}
    <div class="admin-access">
        <a href="{{ path('app_admin_dashboard') }}" class="btn btn-admin">
            👑 Accéder au Dashboard Admin
        </a>
    </div>
{% endif %}
{% endblock %}

